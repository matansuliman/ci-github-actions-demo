name: Nightly Scheduled Build

on:
  schedule:
    - cron: "0 0 * * *"
jobs:

  midnight-run:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v3

      - name: print message
        run: |
          echo "Scheduled build completed successfully!"
